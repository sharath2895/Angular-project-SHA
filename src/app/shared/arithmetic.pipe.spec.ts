import { ArithmeticPipe } from './arithmetic.pipe';

describe('ArithmeticPipe', () => {
  it('create an instance', () => {
    const pipe = new ArithmeticPipe();
    expect(pipe).toBeTruthy();
  });
});
